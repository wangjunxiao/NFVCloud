\href{https://travis-ci.org/mdevils/node-html-entities}{\tt } \href{https://coveralls.io/github/mdevils/node-html-entities?branch=master}{\tt }

Fast html entities library.

\subsection*{Installation }


\begin{DoxyCode}
$ npm install html-entities
\end{DoxyCode}


\subsection*{Usage }

{\bfseries X\+ML entities}

H\+T\+ML validity and X\+SS attack prevention you can achieve from Xml\+Entities class.


\begin{DoxyCode}
const Entities = require('html-entities').XmlEntities;

const entities = new Entities();

console.log(entities.encode('<>"\(\backslash\)'&©®')); // &lt;&gt;&quot;&apos;&amp;©®
console.log(entities.encodeNonUTF('<>"\(\backslash\)'&©®')); // &lt;&gt;&quot;&apos;&amp;&#169;&#174;
console.log(entities.encodeNonASCII('<>"\(\backslash\)'&©®')); // <>"\(\backslash\)'&©®
console.log(entities.decode('&lt;&gt;&quot;&apos;&amp;&copy;&reg;&#8710;')); // <>"'&&copy;&reg;∆
\end{DoxyCode}


{\bfseries All H\+T\+ML entities encoding/decoding}


\begin{DoxyCode}
const Entities = require('html-entities').AllHtmlEntities;

const entities = new Entities();

console.log(entities.encode('<>"&©®∆')); // &lt;&gt;&quot;&amp;&copy;&reg;∆
console.log(entities.encodeNonUTF('<>"&©®∆')); // &lt;&gt;&quot;&amp;&copy;&reg;&#8710;
console.log(entities.encodeNonASCII('<>"&©®∆')); // <>"&©®&#8710;
console.log(entities.decode('&lt;&gt;&quot;&amp;&copy;&reg;')); // <>"&©®
\end{DoxyCode}


{\bfseries Available classes}


\begin{DoxyCode}
const XmlEntities = require('html-entities').XmlEntities, // <>"'& + &#...; decoding
      Html4Entities = require('html-entities').Html4Entities, // HTML4 entities.
      Html5Entities = require('html-entities').Html5Entities, // HTML5 entities.
      AllHtmlEntities = require('html-entities').AllHtmlEntities; // Synonym for HTML5 entities.
\end{DoxyCode}


Supports four methods for every class\+:


\begin{DoxyItemize}
\item encode — encodes, replacing characters to its entity representations. Ignores U\+TF characters with no entity representation.
\item encode\+Non\+U\+TF — encodes, replacing characters to its entity representations. Inserts numeric entities for U\+TF characters.
\item encode\+Non\+A\+S\+C\+II — encodes, replacing only non-\/\+A\+S\+C\+II characters to its numeric entity representations.
\item decode — decodes, replacing entities to characters. Unknown entities are left as is. 
\end{DoxyItemize}